<Window x:Class="Velyka1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Velyka1"
        mc:Ignorable="d"
        Title="MainWindow" Height="370.814" Width="259.559">
    <Grid HorizontalAlignment="Left" Width="326" Height="391" VerticalAlignment="Top">
        <Grid HorizontalAlignment="Left" Height="202" Margin="10,76,0,0" VerticalAlignment="Top" Width="275">
            <DatePicker HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="124" SelectedDate="{Binding Birthday,  Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock HorizontalAlignment="Left" Margin="10,34,0,0" TextWrapping="Wrap" Text="{Binding UpdateSourceTrigger=PropertyChanged, Path=Wishes}" VerticalAlignment="Top"/>
            <TextBlock HorizontalAlignment="Left" Margin="10,50,0,0" TextWrapping="Wrap" Text="{Binding Age}" VerticalAlignment="Top"/>
            <TextBlock HorizontalAlignment="Left" Margin="10,71,0,0" TextWrapping="Wrap" Text="{Binding SunSign}" VerticalAlignment="Top"/>
            <TextBlock HorizontalAlignment="Left" Margin="10,92,0,0" TextWrapping="Wrap" Text="{Binding ChineseSign}" VerticalAlignment="Top"/>
        </Grid>

    </Grid>
</Window>
